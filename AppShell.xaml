<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CQApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CQApp"
    xmlns:views="clr-namespace:CQApp.Views"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
Title="Fiber"
ContentTemplate="{DataTemplate views:TipCalculator}" />-->

    <!--<ShellContent
Title="Fiber"
ContentTemplate="{DataTemplate views:NotesPage}" />-->
    
    <!--<ShellContent
Title="Fiber"
ContentTemplate="{DataTemplate views:LoginPage}" />-->
    
    <!--<ShellContent
Title="FiberLine"
ContentTemplate="{DataTemplate views:FiberLinePage}" />-->

    <!--<ShellContent
    Title="Fiber"
    ContentTemplate="{DataTemplate views:FiberListPage}"
    Route="FiberList" />-->

    <ShellContent
        Title="Cáp quang Đài VT ĐNG"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
</Shell>
